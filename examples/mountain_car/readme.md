# Interactive Mountain Car

To run: `python interactively_trainable_agent.py`

Controls:

- Left:     Left Arrow
- Right:    Right Arrow
- NoOp:     Spacebar

Note:

Currently there are issues that might make this fail on MacOS. WE are looking into them and will update things once they are fixed.